version: '3.8'

services:
  app:
    container_name: app
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/code
      - ${PIP_CACHE_DIR_APP:-cache-app}:/root/.cache
    ports:
      - "8000:8000"
    tty: true

volumes:
  cache-app: